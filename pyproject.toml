[tool.poetry]
name = "football-league-fixtures-forecaster"
version = "0.1.0"
description = "Football fixtures prediction and league table forecasting app"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
requests = "^2.34.2"
pandas = "^3.0.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
